# ==========================================
# 1. INFRASTRUCTURA (DB, RABBIT, MINIO, KEYCLOAK)
# ==========================================

# --- PostgreSQL ---
# Atentie: Backendul se asteapta la portul 5433 extern, dar intern e 5432
DB_USER=keycloak
DB_PASSWORD=change_me_secret_password
DB_NAME=fbi
DB_PORT_EXTERNAL=5433

# --- RabbitMQ ---
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_QUEUE_ANALYSIS=face-analysis-queue
RABBITMQ_QUEUE_FINISHED=analysis-finished-queue

# --- MinIO (S3) ---
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=change_me_minio_password
MINIO_BUCKET=fbi2
# URL-uri pentru comunicare interna vs externa
MINIO_INTERNAL_ENDPOINT=http://minio:9000
# Pentru Local: http://localhost:9000/files | Pentru Prod: https://your-domain.com/files
MINIO_PUBLIC_URL=https://your-domain.com/files

# --- Keycloak ---
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_me_admin_password
KEYCLOAK_DB_NAME=keycloak
KEYCLOAK_REALM=dotnet
KEYCLOAK_CLIENT_ID=dotnet
# Secretul se ia din consola Keycloak -> Clients -> Credentials
KEYCLOAK_CLIENT_SECRET=change_me_client_secret_from_keycloak

# ==========================================
# 2. SERVICII APLICATIE (Backend, Python, FileAPI)
# ==========================================

# --- API Keys & Secrets ---
API_KEY_SHARED=change_me_shared_api_key
FACE_RECOG_API_KEY=change_me_face_recognition_key

# ==========================================
# 3. FRONTEND URLS (Browser Side)
# ==========================================
# In productie inlocuieste 'your-domain.com' cu domeniul real.
# In development local foloseste 'localhost:7002' etc.

NEXT_PUBLIC_API_URL=https://your-domain.com/api
NEXT_PUBLIC_WEBSOCKETS_URL=https://your-domain.com/hubs/surveillance
NEXT_PUBLIC_FILE_API=https://your-domain.com/upload
NEXT_PUBLIC_FILE_API_KEY=change_me_shared_api_key